interface HeroSectionProps {
  onScrollDown?: () => void;
}

export default function HeroSection(_props: HeroSectionProps) {
  // Hero section has been replaced by the Home Eye Checkup promotional section at the top of the page.
  return null;
}
